{
    repository(owner: "{{owner}}", name: "{{repo}}") {
        pullRequest(number: $[[pull_request_number_list]]) {
            databaseId
            number
            baseRef {
                name
            }
            headRef {
                name
            }
        }
        databaseId
    }
}
