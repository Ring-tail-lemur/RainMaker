{
    repository(owner: "{{owner_name}}", name: "{{repository_name}}") {
        refs(last: 100, refPrefix: "refs/heads/") {
            nodes {
                name
                target {
                    ... on Commit {
                        author {
                            user {
                                databaseId
                            }
                        }
                    }
                }
            }
        }
        databaseId
    }
}
