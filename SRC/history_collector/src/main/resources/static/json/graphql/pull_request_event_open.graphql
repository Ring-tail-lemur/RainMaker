{
    repository(owner: "{{owner}}", name: "{{repo}}") {
        pullRequest(number: $[[opened_pull_request_number_list]]) {
            databaseId
            createdAt
            author {
                ... on User {
                    databaseId
                }
            }
        }
    }
}
