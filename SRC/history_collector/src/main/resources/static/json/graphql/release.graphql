{
    repository(owner: "{{owner_name}}", name: "{{repository_name}}") {
        releases(last: 100) {
            nodes {
                databaseId
                name
                author {
                    databaseId
                }
                publishedAt
                tagName
                isPrerelease
                isDraft
            }
        }
        databaseId
    }
}
