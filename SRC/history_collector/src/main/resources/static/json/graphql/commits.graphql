{
    repository(owner: "{{owner}}", name: "{{repo}}") {
        defaultBranchRef {
            target {
                ... on Commit {
                    history{
                        nodes {
                            id
                            committedDate
                            author {
                                user {
                                    databaseId
                                }
                            }
                            message
                        }
                        pageInfo {
                            endCursor
                            hasNextPage
                        }
                    }
                }
            }
        }
    }
}
