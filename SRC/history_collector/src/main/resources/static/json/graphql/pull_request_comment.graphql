{
    repository(owner: "{{owner_name}}", name: "{{repository_name}}") {
        pullRequests(last: 100) {
            nodes {
                comments(last: 100) {
                    nodes {
                        publishedAt
                        databaseId
                        author {
                            ... on User {
                                databaseId
                            }
                        }
                    }
                }
                databaseId
            }
        }
    }
}
