{
    repository(owner: "Ring-tail-lemur", name: "RainMaker") {
        pullRequests(last: 100, states: MERGED) {
            nodes {
                databaseId
                closedAt
                author {
                    ... on User {
                        databaseId
                    }
                }
            }
        }
    }
}
